# Legacy PAPeR help topics

These partials were archived during the Simple CMS reset. They are kept for reference only and are **not** loaded by the in-app help system (`help_page_partial()` only scans `../pages/`).

Simple CMS help topics live in `../pages/`.
