{
  "date": "2026-05-29",
  "title": "Profile invitation card E2E, backup/restore, structure GPS text columns",
  "summary": "Added Playwright coverage for invitation RSVP/distribution UI and persistence; aligned API profile update with web behavior when visit_* keys are omitted; backup manifest schema snapshot and post-restore migrate with round-trip CLI test; structure GPS original text columns (migration 065) alongside decimal map coordinates.",
  "changes": [
    {
      "area": "tests/e2e",
      "items": [
        "profile-invitation-card.spec.ts: RSVP enable/disable matrix, distribution Others visibility, create/view/edit/API persistence, legacy visit clearing when visit_* keys omitted",
        "invitation-card-helpers.ts: shared constants and form helpers",
        "profile-field-visits-crud-history.spec.ts: list soft-delete/restore without visit inputs on form"
      ]
    },
    {
      "area": "App/Controllers/Api/ProfileController.php",
      "items": [
        "buildProfilePayload clears visit_1_* … visit_3_* and invitation visit-status fields when no visit_* keys are present on update (Save main / partial update parity with web gatherProfileData)"
      ]
    },
    {
      "area": "cli backup/restore",
      "items": [
        "backup_schema_helper.php: manifest schema snapshot and post-restore migration runner",
        "backup.php manifest schema + restore.php post-restore migrate (default), --skip-safety-backup, --no-migrate",
        "tests/cli/backup_restore_invitation_roundtrip_test.php (npm run test:backup-restore)"
      ]
    },
    {
      "area": "docs",
      "items": [
        "API_CONTRACT.md: documented legacy visit clearing rule on POST /api/profile/update/{id}",
        "CHANGES.md: E2E, API visit-clearing, and backup/restore notes",
        "README.md: post-restore migrate and test:backup-restore"
      ]
    },
    {
      "area": "structure GPS text",
      "items": [
        "migration_065_structure_gps_text.php: gps_latitude_text, gps_longitude_text",
        "Structure::expandTaggingPayload saves raw text + parsed decimals; gpsCoordinateDisplay / gpsMapLinkFromStructure",
        "Forms, view, PDF, API GET include text fields; maps use decimals"
      ]
    }
  ]
}
