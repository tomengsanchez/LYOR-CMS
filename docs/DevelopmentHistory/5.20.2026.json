{
  "date": "2026-05-20",
  "summary": "Configurable uploads_path for serve/upload handlers when deploy ROOT differs from the directory that holds uploaded files.",
  "changes": [
    {
      "area": "config",
      "description": "Optional uploads_path in config/app.php (see app-sample.php); bootstrap defines UPLOADS_ROOT."
    },
    {
      "area": "uploads",
      "description": "App/UploadPaths centralizes structure, profile, and grievance filesystem paths; controllers use it instead of __DIR__-relative paths."
    }
  ]
}
