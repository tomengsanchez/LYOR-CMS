/**
 * The visual builder was split (see public/assets/js/builder/*.js).
 * Do not run this against the boot editor.js — it would destroy the modules.
 */
console.error('Builder JS is already split. Edit the files in public/assets/js/builder/ instead.');
process.exit(1);
